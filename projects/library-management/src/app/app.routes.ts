import { Routes } from '@angular/router';
import { LibraryComponent } from '../../../library-management-smart/src/public-api';

export const routes: Routes = [{ path: '', component: LibraryComponent }];
